$(function(){
	$('.slide_wrapper').slick({
		autoplay:true,
		autoplaySpeed:3000,
		speed:400,
		dots: true,
	});
	$('.slide_second_wrapper').slick({
		autoplay:true,
		autoplaySpeed:3000,
		speed:400,
	});
});